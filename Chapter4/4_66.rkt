#lang racket
;; Ben has just realized that the logic language has no mechanism for handling duplicates.
;; One way to handle this would be to implement a uniqueness filter.

;; (unique <key-by>)

;; This is a stateful computation to find and remove duplicates.
